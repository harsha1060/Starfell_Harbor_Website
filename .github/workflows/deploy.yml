- name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          # Note: Image name must be lowercase even if repo has uppercase/underscores
          tags: ghcr.io/harsha1060/starfell_harbor_website:latest
